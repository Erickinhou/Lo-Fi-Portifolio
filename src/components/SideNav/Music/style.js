import styled from 'styled-components';

const MusicArea = styled.div`
  display: flex;
  width: 100%;
  justify-content: space-evenly;  
  & > div{
    cursor: pointer;
  }
`;

export { MusicArea };
